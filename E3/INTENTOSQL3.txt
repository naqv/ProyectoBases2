
CREATE TYPE coordenada_t AS type(??


/////se crea el tipo coordenada

)


CREATE TYPE ruta_t AS OBJECT(
  nombre          VARCHAR(100), 
  inicioLatitud   coordenada_t,
  inicioLongitud  coordenada_t,
  finLatitud      coordenada_t,
  finLongitud     coordenada_t,
  cantidadVisitas INTEGER(100),
  fechaIngreso    DATE,
  descripcion     TEXT);
  
  
 
  CREATE TYPE via_t AS OBJECT(
  nombre           VARCHAR(100)
  tipo:            ?????????
  tiposTransporte   (multivaluado)
  inicioLatitud     coordenada_t,
  inicioLongitud    coordenada_t,
  finLatitud        coordenada_t,
  finLongintud      coordenada_t);
  
  
  CREATE TYPE hito-t AS OBJECT(
  
  nombre       VARCHAR(100)
  latitud      coordenada_t
  longitud     coordenada_t
  altitud      coordenada_t
  descripcion  TEXT
  estado
  categoria
  edadApropiada   INTEGER(10)
  paginaweb       
  telefono     ...
  email          ...
  tarifadescuent    ....
  costoTicketMonedaLocal .
  fotoDireccion
  horario        ....
  )
  
  
  CREATE TYPE Evento_t AS OBJECT( 
  nombre  VARCHAR(100),
  categoria
  costoMonedaLocal
  descripcion  TEXT
  
  );
  
  
  CREATE TYPE AS Ciudad_t AS OBJECT (
  nombre  VARCHAR(100),
  pais    VARCHAR(100),
  nombreMonedaLocal
  relacionLocalUSD
  
  
  );
  
  CREATE TYPE Servicio_T AS OBJECT (  
  nombre  VARCHAR(100),
  categoria
  numeroDeContacto
  emial
  paginaweb
  descripcion
  dirccion
  esGratuito
  latitud     coordenada_t,
  longitud    coordenada_t);
  
  CREATE TYPE paquete_t AS OBJECT (
  nombre          VARCHAR(100),
  costoEstudiante
  costoEstandar
  costoTerceraEdad
  costoNIno
  descripcion    TEXT);
  
  CREATE TABLE usuario_t AS OBJECT(
  
  alias   VARCHAR(100)
  nombres ..PODRIAMOS CREAR UN TYPE NOMBRE QUE TENGA YA NOMBRE Y APELLIDO...
  apellidos
  email
  contrasena
  genero
  fechaNacimiento  DATE,
  esEstudiante     BOOLEAN,
  interes
  biografia        TEXT);
  
  CREATE TABLE valoracion_t AS OBJECT (
  
  nivelSeguridad
  relacionPrecioValor
  puntuacion    
  comentario   TEXT);
  